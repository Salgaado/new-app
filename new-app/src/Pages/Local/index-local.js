import React from "react";
import {LocalContainer} from './styled-local';
import LocalTitle from "./components/Header";

export default function Local() {
  return (
    <LocalContainer id="local">
      <LocalTitle/>
    </LocalContainer>
  );
}
